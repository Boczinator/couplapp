import { client } from "./client"

/**
 * Update profile information.
 * @param {string} id - Id of profile
 */
export const getProfile = async (id: string) => {
    try {
        const profile = client.get('profiles/')
    }
}
